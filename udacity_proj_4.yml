apiVersion: v1
kind: Service
metadata:
  name: udacity_proj_4
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8080
  selector:
    app: udacity_proj_4
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: udacity_proj_4
spec:
  replicas: 3
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 2
      maxSurge: 2
  selector:
    matchLabels:
      app: udacity_proj_4
  template:
    metadata:
      labels:
        app: udacity_proj_4
    spec:
      containers:
        - name: udacity_proj_4
          image: CONTAINER_IMAGE
          securityContext:
            privileged: false
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
          ports:
            - containerPort: 8080